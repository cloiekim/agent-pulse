# Agent Pulse OS — 단계별 로드맵

Swift 경험이 거의 없는 상태에서 출발한다는 전제로 짰습니다.
각 단계는 **끝났는지 아닌지를 스스로 판정할 수 있는 기준**을 갖고 있습니다.

전체 흐름: 환경 → 화면 → 감지 → 알림 → 브라우저 → 출시

---

## Phase 0 — 환경 (반나절)

| # | 할 일 | 완료 기준 |
|---|---|---|
| 0.1 | Xcode 설치 (App Store) + 최초 실행해 컴포넌트 설치 | `xcodebuild -version` 이 나옴 |
| 0.2 | `xcode-select --install` | `git`, `swift` 동작 |
| 0.3 | `brew install jq` | 훅 설치 스크립트가 요구함 |
| 0.4 | 이 패키지 압축 해제 후 `swift run AgentPulse --demo` | **메뉴바에 파형 아이콘이 뜨고, 클릭하면 세션 5개가 보임** |

> 0.4 에서 컴파일 에러가 날 수 있습니다. 이 코드는 Swift 툴체인 없는 환경에서
> 작성돼 컴파일 검증을 못 했습니다. 에러는 대부분 import 나 타입 어노테이션 수준이고,
> Xcode 가 대개 자동 수정을 제안합니다. **여기서 막히면 에러 전문을 그대로 물어보세요.**

**Apple Developer Program($99/년)은 지금 사지 마세요.** Phase 5 에서 필요합니다.

---

## Phase 1 — 화면부터 완성 (2~4일)

데이터 없이 UI 를 먼저 끝냅니다. 목업으로 모든 상태를 눈으로 확인할 수 있어야
그 뒤 작업이 빨라집니다.

| # | 할 일 | 완료 기준 |
|---|---|---|
| 1.1 | `--demo` 로 4a(목록) 화면이 디자인과 일치하는지 대조 | 스크린샷을 나란히 놓고 어긋난 곳이 없음 |
| 1.2 | 라이트/다크 모드 둘 다 확인 (시스템 설정에서 전환) | 두 모드 모두 디자인과 일치 |
| 1.3 | `SessionStore.demo()` 를 고쳐 빈 상태(5b)·온보딩(5c) 확인 | 세 화면 전환이 됨 |
| 1.4 | Figtree 폰트 번들링 | 시스템 폰트가 아니라 Figtree 로 렌더됨 |
| 1.5 | 6행 초과 시 "Show N more" 동작 확인 | 접기/펼치기가 됨 |

**Figtree 번들링 방법**: [Google Fonts](https://fonts.google.com/specimen/Figtree)에서 `.ttf`
받아 `Sources/AgentPulse/Resources/Fonts/` 에 넣고, Xcode 프로젝트로 전환한 뒤
Info.plist 에 `ATSApplicationFontsPath = Fonts` 추가.

---

## Phase 2 — 터미널 감지 (3~5일) ⭐ 가장 중요

여기가 제품의 실체입니다. 여기까지만 돼도 쓸 만한 물건이 됩니다.

| # | 할 일 | 완료 기준 |
|---|---|---|
| 2.1 | `swift run AgentPulse` 후 `~/.agent-pulse/token` 생성 확인 | 파일 존재, 권한 600 |
| 2.2 | `./scripts/install-claude-hooks.sh` 실행 | `~/.claude/settings.json` 에 훅이 들어감 |
| 2.3 | 새 터미널에서 Claude Code 실행 → 아무거나 시키기 | **메뉴바 숫자가 1로 바뀜** |
| 2.4 | 위험한 명령을 시켜 승인 프롬프트 띄우기 | **2~3초 안에 노란 점 + 노란 행** |
| 2.5 | Codex `notify` 설정 후 같은 테스트 | Codex 세션도 목록에 나타남 |
| 2.6 | 세션 2개 이상 동시 실행 | 각각 구분돼 보이고 정렬이 맞음 |

**2.4 가 이 제품의 존재 이유입니다.** 여기서 60초가 아니라 2~3초 안에 뜨는지 꼭 확인하세요.
안 되면 `PendingToolTracker.swift` 의 `graceInterval` 을 조정합니다.

### 디버깅 요령

```bash
# 서버가 살아있는지
lsof -nP -iTCP:8787 -sTCP:LISTEN

# 훅을 수동으로 흉내내기
curl -X POST http://127.0.0.1:8787/hook/claude \
  -H "Content-Type: application/json" \
  -H "X-Agent-Pulse-Token: $(cat ~/.agent-pulse/token)" \
  -d '{"session_id":"test","hook_event_name":"Notification","notification_type":"permission_prompt","cwd":"'$PWD'"}'

# 앱 로그
log stream --predicate 'eventMessage CONTAINS "[AgentPulse]"' --level debug
```

---

## Phase 3 — 알림 + 복귀 (2~3일)

**여기서 제품이 실제로 팔리는 물건이 됩니다.** 리서치 결론: 사람들이 돈을 내는 건
알림이 아니라 **"정확히 그 자리로 돌아가기"** 입니다.

| # | 할 일 | 완료 기준 |
|---|---|---|
| 3.1 | 알림 권한 요청 동작 확인 | 시스템 알림이 뜸 |
| 3.2 | 승인 대기 알림이 집중 모드를 뚫는지 | 집중 모드 켜고 테스트 |
| 3.3 | 완료 알림은 집중 모드를 안 뚫는지 | `.passive` 확인 |
| 3.4 | 알림 클릭 → 터미널 앱이 앞으로 옴 | 동작함 |
| 3.5 | **정확한 pane 복귀** — 훅에서 `$TMUX_PANE`/`tty`/`$TERM_PROGRAM` 수집 | tmux 에서 정확한 pane 으로 이동 |
| 3.6 | 방치된 승인 대기 재알림 (`renudge`) | 2분 후 다시 알림 |

**3.5 가 차별점입니다.** 경쟁 제품 대부분이 "앱"까지만 갑니다.
tmux 는 `tmux switch-client -t <pane>` 으로 권한 없이 깔끔하게 됩니다 — 여기부터 시작하세요.

**⚠️ 알림 설계가 곧 유지율입니다.** `running` 으로 알림을 보내면 사용자가 앱을 끕니다.

---

## Phase 4 — 크롬 확장 (5~7일)

PRD 의 Build Order 판단(브라우저 먼저, iOS 나중)은 정확합니다.
**이게 유일한 진짜 차별점입니다** — 이걸 하는 제품이 하나도 없습니다.

| # | 할 일 | 완료 기준 |
|---|---|---|
| 4.1 | MV3 확장 스캐폴드 + `chrome://extensions` 개발자 모드 로드 | 아이콘이 뜸 |
| 4.2 | `fetch` / `EventSource` 래핑으로 SSE 스트림 감지 | 콘솔에 시작/종료 로그 |
| 4.3 | 로컬 서버로 POST (토큰 포함) | 메뉴바에 브라우저 세션이 나타남 |
| 4.4 | claude.ai + chatgpt.com 둘 다 | 두 사이트 모두 동작 |
| 4.5 | 탭 클릭 시 해당 대화로 이동 | 동작함 |

### 🚨 절대 DOM 을 감시하지 마세요

기존 ChatGPT 완료 알림 확장은 **전멸했습니다** — 현존 최대가 사용자 521명 /
별점 2.7 / 2023년 9월 이후 미업데이트, 3개는 스토어에서 삭제됐습니다.
전부 `result-streaming` 같은 CSS 클래스를 폴링했고, UI 개편 때마다 죽었습니다.

살아남은 것들(Claude Usage Tracker 사용자 **10만 명**, 별점 4.8)은 전부
**네트워크 응답 / SSE 스트림을 가로챕니다.** DOM 은 UI 를 *주입*할 때만 씁니다.

참고 구현: [`she-llac/claude-counter`](https://github.com/she-llac/claude-counter)

---

## Phase 5 — 출시 (1~2주)

| # | 할 일 | 비고 |
|---|---|---|
| 5.1 | SPM → Xcode 프로젝트로 전환 | 서명/공증에 필요 |
| 5.2 | Apple Developer Program 가입 | $99/년 |
| 5.3 | 코드 서명 + 공증(notarization) | 없으면 사용자가 앱을 못 엶 |
| 5.4 | 로그인 시 자동 실행 (`SMAppService`) | |
| 5.5 | 랜딩 페이지 + 결제 (Paddle / Lemon Squeezy) | ~$19 이상이면 MAS(15%)보다 수수료가 쌈 |
| 5.6 | 자동 업데이트 ([Sparkle](https://sparkle-project.org/)) | |

### 배포 채널

**Mac App Store 대신 직접 배포를 권합니다.**
MDM 으로 관리되는 회사 Mac 은 IAP 를 못 씁니다. 그런데 이 앱의 주 고객은
**회사가 Claude Max / ChatGPT Pro 를 결제해주는 개발자**입니다.
직접 판매 SKU 가 없으면 그 시장이 통째로 막힙니다.

### 가격 (마켓 분석 결론)

PRD 초안의 $4.99/월($59.88/년)은 **시장가의 2.6~4배**입니다.
검증된 직접 경쟁 제품은 전부 $3~19 일회성이고, 천장이 $24/년 또는 $49~59 일회성입니다.
HN 은 단일 목적 메뉴바 앱의 구독을 **금액과 무관하게 카테고리로 거부**합니다.

**권장:** 무료 코어 + **$29 일회성 Pro**(2대 Mac, 평생 업데이트)
\+ 선택적 $19/년 업데이트 갱신. 클라우드가 실제로 필요해지는 V2(크로스 디바이스 싱크,
푸시)에서만 구독을 붙이면 정당성이 생깁니다.

참고: 메뉴바 윈도우 매니저 "Wins"는 $6.99 에 15개월간 1,612장 = 약 $9,485 를
벌었고, HN 총평은 만장일치로 "너무 싸게 팔았다"였습니다.
**$7 이 아니라 $19~29 에 파는 것이 어떤 기능보다 결과를 많이 바꿉니다.**

---

## 무엇을 MVP 에서 **빼야** 하는가

| 항목 | 판단 | 이유 |
|---|---|---|
| **Usage Dashboard** | V1 → 무료 기능으로 강등, 헤드라인 금지 | CodexBar(무료·MIT·★14.5k·59 프로바이더)와 정면충돌 |
| **완료 알림을 셀링 포인트로** | 빼기 | peon-ping 이 HN 1,006점을 무료로 가져감 |
| **Approve 버튼의 실제 승인** | MVP 는 `Jump` 로 | 훅으로는 불가능. README §4 참고 |
| **Usage Insights / 요금제 추천** | V1.5 → **V2** | 30일 데이터가 쌓여야 의미가 있음 = 출시 후 최소 한 달 |
| **iOS 앱** | V2 유지 (PRD 판단 정확) | 클라우드·인증·푸시가 선행돼야 함 |

---

## 모델 라우팅 (별도 논의 요약)

**MVP·V1 에는 LLM 이 필요 없습니다.** 상태 감지·쿼터 파싱·알림·딥링크는
전부 결정론적 코드입니다. LLM 을 넣으면 이 제품의 핵심 가치인
*"지금 무슨 일이 일어나는지 정확히"* 가 흔들립니다.

LLM 이 필요한 지점은 둘뿐입니다:

1. **Last task summary** (V1) — 파일명·도구 카운트만 보내고 한 문장 생성
2. **Usage Insights / 요금제 추천** (V2) — 집계만 보내고 문장화

둘 다 **Apple Foundation Models framework**(macOS 26 온디바이스)를 권합니다.
추론 비용 $0, 네트워크 0회, PRD 의 local-first 원칙과 완전 일치.
$29 일회성 가격에서 서버 추론 비용은 마진을 갉아먹고,
"프롬프트를 저장하지 않는다"는 약속과도 충돌합니다.

라우팅 기준은 **토큰 수가 아니라 작업 종류**여야 합니다:

| 작업 | 모델 | 빈도 |
|---|---|---|
| 사실 → 문장 변환 | 온디바이스 / Haiku급 | 실시간 |
| 30일 패턴에서 비정형 인사이트 | Sonnet급 | 월 1회 |

핵심 원칙: **결정론적으로 계산하고, 언어만 싸게 생성한다.**
"Codex 사용률 20% 미만 → 월 $15 절약"은 추론이 아니라 산수입니다.

---

## 예상 일정

| Phase | 기간 | 누적 |
|---|---|---|
| 0. 환경 | 0.5일 | 0.5일 |
| 1. 화면 | 2~4일 | ~4일 |
| 2. 터미널 감지 | 3~5일 | ~9일 |
| 3. 알림 + 복귀 | 2~3일 | ~12일 |
| 4. 크롬 확장 | 5~7일 | ~19일 |
| 5. 출시 | 7~14일 | **~4~5주** |

Swift 초보 기준이고, 하루 3~4시간 작업 가정입니다.
**Phase 3 끝나면 이미 본인이 매일 쓸 수 있는 물건이 됩니다.**
거기서 2주쯤 직접 써보고 Phase 4 로 갈지 판단하세요.
